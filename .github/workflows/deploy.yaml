name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t ajpcorrea/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true ajpcorrea/react-test npm test

      - run: docker build -t ajpcorrea/multi-client ./client
      - run: docker build -t ajpcorrea/multi-nginx ./nginx
      - run: docker build -t ajpcorrea/multi-server ./server
      - run: docker build -t ajpcorrea/multi-worker ./worker

      - run: docker push ajpcorrea/multi-client
      - run: docker push ajpcorrea/multi-nginx
      - run: docker push ajpcorrea/multi-server
      - run: docker push ajpcorrea/multi-worker
